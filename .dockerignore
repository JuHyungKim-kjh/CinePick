# 비밀 설정. 이미지 안으로 절대 들어가면 안 된다 —
# 이미지를 Docker Hub 에 올리는 순간 그 안의 파일도 함께 공개된다.
config/
.env
.env.*

# 빌드 산출물. 이미지 안에서 새로 빌드하므로 로컬 것을 넣으면 오히려 캐시를 망친다
build/
.gradle/
out/
bin/

# 개발 환경 부산물
.git/
.gitignore
.idea/
.vscode/
*.iml

# 문서·이미지. 실행에 쓰이지 않는데 빌드 컨텍스트만 키운다
*.md
*.png
*.PNG
*.zip
